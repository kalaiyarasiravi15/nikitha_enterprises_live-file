{
  "name": "panel-runner",
  "private": true,
  "scripts": {
    "dev": "cd admin_dashboard && npm run dev",
    "start": "cd admin_dashboard && npm run dev",
    "build": "cd admin_dashboard && npm run build",
    "install": "cd admin_dashboard && npm install"
  }
}
